# Python 包管理

Python 包管理、依赖解析、虚拟环境和锁文件的指南。

## 结构

| 文件 | 用途 |
|------|---------|
| `SKILL.md` | Agent 技能定义（frontmatter + 指令） |
| `metadata.json` | 机器可读元数据和版本控制 |
| `AGENTS.md` | Agent 优化的快速参考（生成） |
| `README.md` | 本文件 |
| `rules/` | 12 个单独的最佳实践规则 |

## 使用

```bash
npx agentskills add Tyler-R-Kendrick/agent-skills/skills/python/package-management
```

## 许可证

MIT
